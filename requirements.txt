# Bewusst EXAKT gepinnt: der GitHub-Actions-Modus installiert diese Pakete bei jedem
# Lauf (48x/Tag) frisch - mit offenen ">="-Angaben wuerde ein beliebiges Breaking-
# Release eines Upstream-Pakets den Monitor von einer Minute auf die andere toeten,
# ohne dass sich am eigenen Code irgendetwas geaendert hat. Die Versionen hier sind
# die, gegen die die Test-Suite (tests/) verifiziert ist. Zum Aktualisieren: Versionen
# anheben, Tests laufen lassen, committen (der Tests-Workflow prueft es automatisch).
fastapi==0.139.0
uvicorn[standard]==0.51.0
httpx==0.28.1
anthropic==0.116.0
python-dotenv==1.2.2
feedparser==6.0.12
pydantic==2.13.4

# Truth-Social Browser-Fallback (empfohlen; TRUTH_SOCIAL_BROWSER_FALLBACK=false zum Abschalten).
# Nach "pip install" zusaetzlich einmalig ausfuehren: playwright install chromium
playwright==1.61.0

# Optional: only needed if ENABLE_LIVE_AUDIO=true
# yt-dlp>=2024.8.6
# faster-whisper>=1.0.3
